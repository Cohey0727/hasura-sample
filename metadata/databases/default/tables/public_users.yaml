table:
  name: users
  schema: public
array_relationships:
- name: todos
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: todos
        schema: public
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns: []
  role: user
select_permissions:
- permission:
    columns:
    - id
    - name
    - created_at
    - last_seen
    filter: {}
  role: user
